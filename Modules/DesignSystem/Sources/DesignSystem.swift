//
//  DesignSystem.swift
//  DesignSystem
//
//  Phase 8 Stage 1: DesignSystem 모듈 부트스트랩 시점의 placeholder 파일.
//  Stage 1-2부터 CommonUI / Extensions / Resources / L10n 등이 이 모듈로 이동하면서
//  이 파일은 삭제될 예정.
//

import Foundation

/// DesignSystem 모듈의 루트 네임스페이스.
/// 모듈이 비어 있으면 Tuist가 생성하는 프레임워크 타겟이 "no source files"로
/// 컴파일 에러를 내기 때문에, 첫 부트스트랩 시점에만 placeholder로 존재한다.
public enum DesignSystem {
    /// 현재 모듈 이름 (디버깅/로깅 용도)
    public static let moduleName = "DesignSystem"
}
